plugins {
    // Lets Gradle auto-provision a JDK 21 toolchain if the build JVM differs
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'prometheus-mcp'
